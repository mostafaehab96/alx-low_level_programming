#include <stdlib.h>

/**
 * _abs - calculate the absolute value of an integer
 * @n: the integer to calculate it's absolute value
 *
 * Return: the absolute value being calculated
 */

int _abs(int n)
{
	return (abs(n));
}
