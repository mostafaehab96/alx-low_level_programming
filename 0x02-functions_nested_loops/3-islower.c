#include <ctype.h>

/**
 * _islower - checks if the letter c is lowercase
 *@c: The character to check
 *
 * Return: 1 if lower, else 0
 */

int _islower(int c)
{
	if islower(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
